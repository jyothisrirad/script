#!/bin/bash

s81=spigot-1.11.2-bedwar-81

s81() {
	oldmcvar=$mcver
	export mcver=$s81 
	
	mcpath=/mnt/runtimes/$mcver
	[ ! -d "$mcpath" ] && { echo -e "${RED}$mcpath not installed${NC}"; mcver=$oldmcvar; }
	[   -d "$mcpath" ] && { echo mcver=$mcver; cd $mcpath; }
}

alias 81=s81
alias bed1=s81
