#!/bin/bash

s76=spigot-1.11.2-Wither-76

# s76() {
	# oldmcvar=$mcver
	# export mcver=$s76 
	
	# mcpath=/mnt/runtimes/$mcver
	# [ ! -d "$mcpath" ] && { echo -e "${YELLOW}$mcpath not installed${NC}"; mcver=$oldmcvar; }
	# [   -d "$mcpath" ] && { echo mcver=$mcver; cd $mcpath; }
# }

s76() {
	chmc $s76
}

alias v11w=s76
alias 76=s76
alias wither=s76
alias wrun='76 && /home/sita/script/minecraft/wither.arena $*'
